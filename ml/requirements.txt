# SOS ML training stack — CPU baselines
# Install torch from the CPU wheel index (keeps images small):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
pandas>=2.0
numpy>=1.24
pytest>=7.0

# Optional extras (install explicitly when needed):
#   pyarrow>=14        # parquet writes; without it generators fall back to CSV
#   torch-geometric    # real SAGEConv layers; without it pure-torch fallback is used
#   ray[train]>=2.9    # --ray scale-out for training
#   mlflow>=2.10       # registry mirror when SOS_MLFLOW_TRACKING_URI is set
