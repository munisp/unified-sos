apiVersion: v2
name: sos-platform
description: >-
  SOS umbrella chart — APISIX gateway, Keycloak per-state realm import,
  SOS module services, Kubecost metering and KEDA autoscaling.
  Deployed once per state tenant via ArgoCD (see infra/gitops/).
type: application
version: 0.2.0
appVersion: "2026.1"
keywords:
  - sos
  - nigeria
  - revenue
  - multi-tenant
maintainers:
  - name: SOS Platform Engineering
    email: platform-eng@sos.gov.ng
sources:
  - https://github.com/munisp/unified-sos
