{
  "clients": [
    {
      "clientId": "sos-services",
      "description": "Service-to-service client for SOS modules. Secret is NOT stored here; it is injected at import time from the external secret reference 'sos-ogun-sos-services'.",
      "directAccessGrantsEnabled": false,
      "enabled": true,
      "protocol": "openid-connect",
      "publicClient": false,
      "serviceAccountsEnabled": true,
      "standardFlowEnabled": false
    },
    {
      "attributes": {
        "pkce.code.challenge.method": "S256"
      },
      "clientId": "sos-citizen-portal",
      "description": "Public citizen portal client — PKCE (S256) only, no client secret by design.",
      "directAccessGrantsEnabled": false,
      "enabled": true,
      "protocol": "openid-connect",
      "publicClient": true,
      "redirectUris": [
        "https://portal.ogun.sos.gov.ng/*"
      ],
      "standardFlowEnabled": true,
      "webOrigins": [
        "https://portal.ogun.sos.gov.ng"
      ]
    }
  ],
  "displayName": "SOS Ogun State",
  "enabled": true,
  "realm": "sos-ogun",
  "roles": {
    "realm": [
      {
        "description": "Platform administrator (SOS Ogun State, hybrid tier)",
        "name": "sos-admin"
      },
      {
        "description": "State IRS revenue officer (SOS Ogun State)",
        "name": "sos-revenue-officer"
      },
      {
        "description": "Auditor-General read-only access (SOS Ogun State)",
        "name": "sos-auditor-readonly"
      },
      {
        "description": "Citizen portal end user (SOS Ogun State)",
        "name": "sos-citizen"
      }
    ]
  },
  "sslRequired": "external"
}
