apiVersion: v2
name: openappsec
description: >-
  OpenAppSec WAF enforcement layer for the SOS platform. Deploys the
  open-appsec agent in front of the APISIX ingress with a default-deny
  posture aligned with the Cilium network matrix in infra/k8s/base.
type: application
version: 0.1.0
appVersion: "1.1.20"
