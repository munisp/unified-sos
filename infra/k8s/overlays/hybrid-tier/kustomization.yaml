apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Tier 2 — Hybrid Industrial overlay (Ogun).
# Dedicated PostgreSQL/PostGIS transactional cluster and dedicated node pool;
# only the Sedona/lakehouse analytical processing is shared.

commonLabels:
  sos.gov.ng/tenancy-tier: hybrid

resources:
  - ../../base
  - states/ogun.yaml
